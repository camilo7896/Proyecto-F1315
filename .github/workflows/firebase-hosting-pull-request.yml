# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. Instalar Node.js
      - uses: actions/setup-node@v4
        with:
<<<<<<< HEAD
          node-version: 20   # o 18, depende de lo que uses en tu proyecto

      # 2. Instalar dependencias
      - run: npm ci

      # 3. Compilar
      - run: npm run build

      # 4. Deploy preview en Firebase Hosting
=======
          node-version: 20 # puedes usar 18 o 20, depende de tu proyecto

      # 2. Instalar dependencias
      - run: npm ci # o "npm install" si no tienes package-lock.json

      # 3. Compilar tu proyecto con Vite
      - run: npm run build

      # 4. Hacer deploy del preview en Firebase Hosting
>>>>>>> b54d5bf (Modificacion en archivos github)
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROYECTOUNO_D09E0 }}
          projectId: proyectouno-d09e0
